export interface IFeaturesListItemData {
  width: number;
  height: number;
  svg: string;
  title: string;
  text: string;
  gap: boolean;
  id: string;
}

export interface IFeaturesText {
  gap?: boolean;
}
